package org.example.torneoajedrez.model;

public class PruebaCodigos {
    /*
     private void initGUI()
     {

            registroTorneo.getSelectionModel().selectedItemProperty().addListener((obs, oldVal, newVal) -> {
        if (newVal != null) {
            registroFormato.setItems(newVal.getFormatoTorneo());
            registroFormato.getSelectionModel().selectFirst(); // selecciona el primero automáticamente
        }
    });

    actions ()
    {
            registroTorneo.setOnAction(event ->
        {
            indice = registroTorneo.selectionModelProperty().get().getSelectedIndex();
            registroFormato.setItems(listaTorneos.get(indice).getFormatoTorneo());
        });
    }

    }*/

}
